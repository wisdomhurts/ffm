// Cloud save UI (save codes). OWNER: backend agent (docs/ONLINE.md). openCloudSave(app).
import { comingSoon } from './comingSoon.js';
export const openCloudSave = (app) => comingSoon(app, 'Cloud Save');
