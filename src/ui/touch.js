// PLACEHOLDER touch controls. Contract: createTouchControls(app) -> { setVisible(bool), update(dt), dispose() }
export function createTouchControls() {
  return { setVisible() {}, update() {}, dispose() {} };
}
